import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
    container: {
        marginVertical: 10,
        flexDirection: 'row',
    },

    avatarView:{
            marginRight: 10,
    },

    userDetails:{
    flex: 1,
    },

})